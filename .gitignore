#-------------------------------------------------------------------------------
# Rust-specific build and environment files
#-------------------------------------------------------------------------------

# The target directory is where Rust puts all compiled artifacts (binaries, libs)
/target/

# Cargo build/test metadata cache
# You generally don't want to ignore this if you use Cargo for testing,
# but it can be safely ignored for simple projects.
# /Cargo.lock

# Build documentation generated by 'cargo doc'
/doc/

# Rust's own dependency build/cache directory (often found in the home directory)
# If your project uses custom build directories outside of `target/`
**/*.d
**/*.rlib
**/*.rmeta

# Crates generated by cargo-vendor
/vendor/

# Rust's coverage files
*.gcno
*.gcda
*.profumo
*.profraw
*.profdata

#-------------------------------------------------------------------------------
# Operating System and Editor/IDE Files
#-------------------------------------------------------------------------------

# macOS
.DS_Store
**/.DS_Store

# Linux
*~
# backups
\#*\#

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Visual Studio Code
.vscode/
# Note: You may want to keep .vscode/settings.json or extensions.json
# if they contain project-specific configuration.
# If so, ignore only the cache and logs:
.vscode/pylint-cache/
.vscode/vscode-RUST-LOG.*

# JetBrains IDEs (IntelliJ, CLion, RustRover)
.idea/
*.iws
*.iml
*.ipr

# Vim/NeoVim swap and backup files
*.swp
*.bak
